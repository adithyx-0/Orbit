<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Usage access: user must grant manually via Settings > Apps > Special app access > Usage access -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:label="Prosit"
        android:theme="@style/Theme.Prosit">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.login.SignupActivity"
            android:exported="false" />

        <activity
            android:name=".ui.subscriptions.SubscriptionsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.goals.GoalsActivity"
            android:exported="false" />

    </application>
</manifest>
