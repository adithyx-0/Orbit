# PROSIT — Android Companion (scaffold pending)

Kotlin companion app that collects per-package foreground usage via
`UsageStatsManager` and pushes aggregated data to the PROSIT backend.

Sprint 2+ work.
