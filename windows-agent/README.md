# PROSIT — Windows Desktop Agent (scaffold pending)

Python agent (`psutil` + `pywin32`) that records active-window / process
foreground time and pushes aggregated hourly usage to the PROSIT backend.

Sprint 2+ work.
